<resources>
    <!-- Tema mínimo válido para Compose -->
    <style name="Theme.LevelUp" parent="android:Theme.Light.NoTitleBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

</resources>
